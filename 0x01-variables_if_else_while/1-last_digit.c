#include<stdio.h>
#include<time.h>
#include<stdlib.h>

/**
 * main - Checks last digit
 * 
 * Return: 0
 */

int main(void)
{
	char ch;
	for (ch = 'a' ; ch = 'z' ; ch++)
	{
			putchar(ch);
	}
	putchar('\n');
	return (0);
}
